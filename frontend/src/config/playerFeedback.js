/**
 * Shared timing for the short-lived creator rename reversal action.
 * Keep this in one place so all creator feedback surfaces agree on the window.
 */
export const CREATOR_RENAME_UNDO_WINDOW_MS = 8_000;
