# Echoo hosted production web build (public Vite variables only).
# Secrets belong in backend/.env and must never be placed in VITE_* variables.
VITE_BUILD_BASE=/
VITE_API_URL=/api
VITE_PUBLIC_APP_ORIGIN=https://echoo.digi02.org
VITE_LIVEKIT_URL=
VITE_SYNTHETIC_AUDIO=false
