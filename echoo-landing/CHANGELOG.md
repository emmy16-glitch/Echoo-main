# Echoo Landing Changelog

## Unreleased

- Enhanced the local curated assistants on the public landing pages, listener workspace, and creator studio with a brief truthful local typing indicator and reduced-motion-safe panel, answer, and consent-panel transitions. The feedback appears only while the browser selects a deterministic local response; it does not make an AI, analytics, API, or background request.
- Added a consent-first human-support control to every assistant dialog. It requires an explicit opt-in before opening a recipient-free, empty email draft with only a fixed subject. The assistant never pre-fills or transmits a visitor question, account detail, room detail, chat data, or message body; the visitor chooses a verified Echoo support recipient and decides whether to send anything from their own mail application.
- Expanded curated local guidance for unsigned desktop-installer warnings and web access issues; listener room-loading/reconnection and playback troubleshooting; and creator microphone permission, studio connection, echo/feedback, and audio-readiness support. All new responses retain the established no-account/no-room-state boundary.
- Added direct deterministic resolver coverage for installer, listener-connection, creator-permission, and recipient-free escalation behavior in both the landing and authenticated application helpers.

- Added a local, deterministic curated-help assistant to both public landing routes for product, web-app, download, release, installer, and early-access guidance. The assistant makes no AI-provider, analytics, or API call and retains guidance only in browser component state for the open session.
- Added direct curated-response and public-control coverage for the public assistant’s key subjects, privacy-safe fallback, keyboard-reachable 44px trigger, and dialog-free initial render. The authenticated application now adds a separate listener-support assistant and creator copilot with equally local, context-specific guidance; they do not access account, room, chat, audience, or playback data.
- Documented the exact managed landing image workflow: current landing URLs are constants in `Home.tsx` and `Release.tsx`, approved original screenshots are staged at `/home/ubuntu/webdev-static-assets/echoo-approved/`, and main-application visual assets remain under `frontend/src/Components/Assets/`.

- Added restrained interaction feedback to compact-menu icon controls: hover and keyboard focus lightly elevate or tint the icon, while pointer press gives a compact active response. The same controls explicitly suppress non-essential transforms for reduced-motion users and retain their visible focus outlines.
- Final validation for this refinement: 17 test files passed with one intentionally skipped inactive-credentials health test (44 passing tests), followed by successful TypeScript checking, production build, restarted phone-width review, and direct static coverage of the hover, active, and reduced-motion class wiring.

- Added disabled, in-button newsletter submission feedback with a labelled spinner and `aria-busy` state, so an explicit consent-backed request has clear progress feedback without permitting duplicate submissions.
- Added decorative, aria-hidden icons to every compact-menu destination and its release action while preserving each link’s existing accessible name, route, overlay, slide-in behavior, and reduced-motion handling.
- Final validation for this refinement: 17 test files passed with one intentionally skipped inactive-credentials health test (44 passing tests), followed by successful TypeScript checking, production build, and a restarted phone-width landing review.

- Refined the compact phone navigation with a smooth top-sheet slide, a semi-transparent deep-cobalt overlay, focus-preserving dialog semantics, and reduced-motion-safe behavior. Direct public-control coverage verifies the panel and overlay treatment alongside the menu’s labelled trigger and routes.
- Successful consent-backed newsletter submissions now show an accessible success toast through the existing notification provider. The toast makes no claim that email delivery is active; the double-opt-in delivery flow remains explicitly disabled until valid verified sender credentials are supplied.
- Final validation for the interaction refinement: 17 test files passed with one credentials-health test intentionally skipped while delivery remains inactive (43 passing tests), followed by successful TypeScript checking, production build, and a restarted phone-width landing review.

- Added a compact, accessible drawer navigation for phone widths. It preserves the public routes, the release action, and theme access without crowding the sticky header; rendered component coverage verifies its trigger, menu landmark, links, and action wiring.
- Replaced the landing’s generic product panels with the user-approved Echoo listener dashboard, sign-in, and creator-studio screenshots, served as deployment-safe responsive assets.
- Implemented a privacy-conscious double-opt-in newsletter architecture: explicit consent and email-format validation precede an opaque, time-limited confirmation link and a recipient confirmation destination. Delivery is deliberately **inactive** until a valid Resend API key, a verified sender domain, and the explicit `NEWSLETTER_DELIVERY_ENABLED=true` flag pass the non-delivery credentials check; no test subscription was stored or emailed.
- Migrated the canonical landing source to `Echoo-main/echoo-landing` alongside the web, mobile, and desktop subprojects. The prior standalone landing checkout remains a local recovery copy during the repository transition; the local design guide now documents the in-repository workflow.
- Final verification for this update: 16 test files passed with one credentials-health test intentionally skipped while delivery is inactive (42 passing tests), followed by successful TypeScript checking, production build, and restarted phone-route reviews.

- Refined dark mode into a deep cobalt Echoo palette rather than black or gray. The saved theme preference carries between the homepage and release bulletin.
- Added a release-specific **Copy Link** action beside the recommended installer and every platform download. It copies only the public v1.0.5 release-page URL and does not collect or transmit visitor data.
- Converted the detailed v1.0.5 notes into an accessible, collapsible accordion so the release page stays scannable while every verified change remains available.
- Verified the updated release bulletin in a browser: the deep-blue theme, default detailed changelog entry, download selector, and copy actions render together with the existing direct-download behavior.
- Reviewed the refreshed release bulletin at narrow phone width and the homepage at desktop width. The responsive accordion, share controls, release cards, and Echoo blue content hierarchy remain readable and reachable.
- Completed an explicit responsive comparison of the final release bulletin in forced light and deep-blue dark themes. The review confirmed the navigation, accordion, share controls, and platform cards at both widths. The narrow dark-mode hero now uses a deep-blue signal surface with high-contrast v1.0.5 heading and supporting copy.
- Re-ran the complete test, type-check, and production build after the dark-hero correction, then restarted the development service cleanly. A fresh clone of the private `effiukp/Echoo-landing` repository also installed and started the documented `dev:design` preview successfully from a separate directory.
- Independently rechecked the final narrow-screen deep-blue release hero after the contrast correction. The v1.0.5 title, release metadata, explanatory copy, and primary download action are readable, while the running development session loads the current release route without an import or build failure.
- Added immediate copy confirmation, a reduced-motion-safe theme fade, and a consent-backed footer newsletter input that reuses the existing release-update subscription flow. Desktop and phone reviews confirm that the new footer form, release Copy Link controls, and accordion remain visible and reachable in the current deep-blue theme.
- Added explicit newsletter success-message coverage without creating test subscriber data, alongside an in-browser validation check. The final phone-width release review confirms that the v1.0.5 bulletin, Copy Link controls, platform cards, and accordion remain readable in both forced light and deep-blue dark themes.
- Added privacy-conscious X, LinkedIn, and WhatsApp share links beside the footer newsletter field, plus a keyboard-accessible Back to Top control for long pages. Desktop and phone reviews confirm the footer share controls and newsletter form remain clear in both themes; Back to Top visibility and scrolling behavior are covered by focused unit tests and respect reduced-motion preferences.
- Confirmed that footer share controls use explicit accessible labels and generate only service-specific links containing the fixed public release URL and product message, with no Echoo sharing telemetry. The Back to Top control is a native button that appears at a 480px scroll threshold and chooses instant scrolling when reduced motion is enabled. The complete suite passed: 13 test files and 33 tests, followed by successful type checking and production build.
- Updated the public headers to remain sticky while scrolling, keeping the theme toggle and release action reachable. Desktop and phone reviews confirm the sticky header, footer newsletter feedback, and branded share controls remain visible and usable in the current deep-blue theme.
- Refined the sticky header across the homepage and release route with theme-aware surfaces, leaving the theme toggle accessible during long-page scrolling. The footer newsletter now gives immediate email-format and consent feedback before using the established consent-backed confirmation. X, LinkedIn, and WhatsApp controls have distinct accessible hover and keyboard-focus colors; desktop and phone light/dark checks confirm the controls remain readable and comfortably tappable.
- Validation note: focused unit coverage exercises the X, LinkedIn, and WhatsApp share URL construction, the Back to Top scroll threshold, and newsletter validation and success-message behavior. Sticky positioning and CSS-only hover colors have no runtime state to unit test in this project; they are verified by the responsive light/dark visual reviews, while native button and link semantics preserve keyboard operation.
- Re-ran the full suite after the sticky-navigation refinement: 13 test files and 34 tests passed, followed by successful type checking and production build. The only matching Vite import entry remains an earlier timestamped log from before `releaseChangelogState.ts` existed; the current type-check, tests, and production build resolve that module successfully.
- Extracted the shared public sticky-navigation shell and footer social-share controls so the homepage and release bulletin retain one accessible, theme-toggle-ready navigation structure and the established branded share-link styling.
- Added direct server-rendered component coverage for sticky-navigation semantics, the real theme-toggle accessible label, the share landmark, all three social-control labels and styling hooks, hardened external-link attributes, fixed public share destinations, and the absence of tracking parameters. CSS-only hover colors remain validated through the responsive light/dark visual review because they have no runtime state.
- Re-ran the full landing suite after the extraction: 14 test files and 36 tests passed, followed by successful type checking and production build. Restarted desktop and phone reviews of both routes in forced light and deep-cobalt dark themes confirm the sticky headers, toggle access, newsletter feedback area, and footer sharing controls remain readable and reachable.
- Manual release-page review confirms that Copy Link controls are present beside the recommended installer and every manual platform option, while the detailed changelog remains independently operable.
- Manual interaction verification confirms that a successful Copy Link click shows the button’s temporary copied state and a “Copied!” toast. The homepage footer exposes a separate future-release email field with an explicit no-third-party-sharing consent checkbox.
- After restarting the local service, the release page rendered the v1.0.5 bulletin and Copy Link controls successfully. The navigation toggle switched the page into the light presentation and updated its accessible label to offer the dark presentation.
- Added a persistent light-and-dark theme preference to the public Echoo navigation. Initial browser review confirms that switching updates the release bulletin and its accessible toggle label without changing release content or routes.
- Added immediate per-asset download feedback: the clicked control announces that it is starting, shows a spinner, and preserves the existing public direct-download behavior and fixed anonymous event tracking.
- Added a detailed v1.0.5 changelog below the release bulletin, covering listener controls, creator broadcast flow, privacy-preserving desktop controls, validated Linux distribution alternatives, and explicit unsigned-delivery guidance.
- Confirmed the dark preference carries from the release bulletin to the homepage in-browser, and reviewed both routes at a narrow phone width for readable content flow and reachable navigation and download controls.
- Reworked the homepage from the supplied visual references into an original Echoo layout, retaining the paired-ellipse logo, routes, and privacy-preserving early-access flow without importing reference code or external reference imagery. The completed refinement uses white and soft Echoo Blue as primary reading surfaces, a deep-blue early-access contrast band, asymmetric editorial splits, a label rail, ruled divisions, and compact release metadata.
- Aligned the visible release bulletin and browser metadata to the validated v1.0.5 desktop delivery. Desktop and phone reviews confirm that the homepage and release selector remain readable, with manual installer options and status guidance available at both widths.
- Added an OS-aware release selector that recommends a Windows installer or Debian package based on the browser platform without initiating any automatic download.
- Added verified Windows x64, Apple-silicon macOS, and Linux v1.0.5 downloads through the landing project’s public delivery path while preserving the private source repository.
- Refreshed the public Linux v1.0.5 targets after a successful Ubuntu build and packaged-shell validation of the `.deb`, AppImage, and `.tar.gz` alternatives.
- Rechecked the `/release` page at desktop and narrow-phone widths; the detected-platform recommendation and all manual download alternatives remain visible and reachable.
- Clearly labelled every current installer as unsigned; the release page explains the expected Windows SmartScreen and macOS Gatekeeper warnings and notes that the macOS DMG is not notarized.
- Added manual alternative choices for all available Windows, macOS, and Linux assets, including AppImage and tar.gz Linux fallbacks.
- Added anonymous per-asset download events for Windows, macOS, and each Linux release link. Tracking emits only a fixed asset event name and does not send visitor identity, platform, or URL parameters.
- Updated the homepage **Get Echoo** call to action to open the release downloads at `/release#downloads` rather than the live Echoo authentication page.
- Kept product-acquisition and release-specific calls to action distinct: release/download actions remain on the landing release route, while the live web application continues at `https://echoo.digi02.org`.
- Preserved the consent-gated, non-identifying early-access analytics and phone-focused interaction safeguards delivered in the current landing build.
